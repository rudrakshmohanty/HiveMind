services:
  mongo:
    image: mongo:7
    container_name: ollama-chat-mongo
    ports:
      - "27017:27017"
    volumes:
      - mongo_data:/data/db
    restart: unless-stopped

  ollama:
    image: ollama/ollama:latest
    container_name: ollama-service
    ports:
      - "11434:11434"
    volumes:
      - ollama_models:/root/.ollama
    environment:
      - OLLAMA_HOST=0.0.0.0:11434
    restart: unless-stopped

  backend:
    image: python:3.12-slim
    container_name: ai-chat-backend
    working_dir: /app
    ports:
      - "8000:8000"
    depends_on:
      - ollama
      - mongo
    environment:
      - OLLAMA_BASE_URL=http://ollama:11434
      - MONGODB_URL=${MONGODB_URL:-mongodb://mongo:27017}
      - MONGODB_DB=${MONGODB_DB:-ollama_chat}
    volumes:
      - ./backend:/app
    command: bash -c "pip install -r /app/requirements.txt && uvicorn main:app --host 0.0.0.0 --port 8000"
    restart: unless-stopped

  frontend:
    image: node:20-slim
    container_name: ai-chat-frontend
    ports:
      - "3000:3000"
    depends_on:
      - backend
    environment:
      - VITE_BACKEND_URL=http://backend:8000
    volumes:
      - ./frontend:/app
    command: bash -c "npm install && npm run dev"
    restart: unless-stopped

volumes:
  ollama_models:
  mongo_data:
